<Window x:Class="Caro_game.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Caro_game.ViewModels"
        mc:Ignorable="d"
        Title="Caro Game"
        Width="1000" Height="700"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}"
        WindowState="Maximized">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Border Background="{DynamicResource SurfaceBackgroundBrush}" CornerRadius="12" Padding="16">
            <TabControl>

                <!-- Chơi -->
                <TabItem Header="▶  Chơi">
                    <Border Background="{DynamicResource SectionBackgroundBrush}" Margin="-4" Padding="16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="280"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Cột trái: tùy chọn -->
                            <Border Grid.Column="0" Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Padding="16">
                                <StackPanel>
                                    <TextBlock Text="Tùy chọn nhanh" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource DefaultForeground}"/>
                                    <Separator Margin="0,8"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <TextBlock Text="Người đi trước:" Width="120" Foreground="{DynamicResource DefaultForeground}"/>
                                        <ComboBox Width="120" ItemsSource="{Binding Players}" SelectedItem="{Binding FirstPlayer}"/>
                                    </StackPanel>

                                    <CheckBox Content="Chơi với máy" Margin="0,10,0,0"
                                              IsChecked="{Binding IsAIEnabled}" Foreground="{DynamicResource DefaultForeground}"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <TextBlock Text="Cấp độ:" Width="120" Foreground="{DynamicResource DefaultForeground}"/>
                                        <ComboBox Width="120"
                                                  ItemsSource="{Binding AIModes}"
                                                  SelectedItem="{Binding SelectedAIMode}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <TextBlock Text="Luật chơi:" Width="120" Foreground="{DynamicResource DefaultForeground}"/>
                                        <ComboBox Width="120"
                                                  ItemsSource="{Binding RuleOptions}"
                                                  SelectedItem="{Binding SelectedRuleOption}"
                                                  DisplayMemberPath="Name"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <TextBlock Text="Thời gian (phút):" Width="120" Foreground="{DynamicResource DefaultForeground}"/>
                                        <ComboBox Width="120"
                                                  ItemsSource="{Binding TimeOptions}"
                                                  SelectedItem="{Binding SelectedTimeOption}"
                                                  DisplayMemberPath="Display"/>
                                    </StackPanel>

                                    <TextBlock Text="{Binding RemainingTimeDisplay, StringFormat=Thời gian còn lại: {0}}"
                                               Margin="0,6,0,0"
                                               Foreground="{DynamicResource MutedForegroundBrush}"/>

                                    <TextBlock Text="{Binding StatusMessage}"
                                               Margin="0,10,0,0"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource AccentForegroundBrush}"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                                        <Button Content="Bắt đầu ván mới"
                                                Width="130"
                                                Background="{StaticResource Primary}" Foreground="{DynamicResource DefaultForeground}"
                                                Command="{Binding StartGameCommand}"/>
                                        <Button Content="{Binding PauseButtonText}"
                                                Width="110"
                                                Margin="10,0,0,0"
                                                Background="{StaticResource Primary}" Foreground="{DynamicResource DefaultForeground}"
                                                Command="{Binding TogglePauseCommand}"/>
                                    </StackPanel>

                                    <Button Content="Lưu ván đấu"
                                            Margin="0,10,0,0"
                                            Background="{StaticResource Primary}" Foreground="{DynamicResource DefaultForeground}"
                                            Command="{Binding SaveGameCommand}"/>
                                    <Button Content="Mở ván đã lưu"
                                            Margin="0,10,0,0"
                                            Background="{StaticResource Primary}" Foreground="{DynamicResource DefaultForeground}"
                                            Command="{Binding LoadGameCommand}"/>
                                </StackPanel>
                            </Border>

                            <!-- Cột phải: bàn cờ -->
                            <Border Grid.Column="1" Background="{DynamicResource SurfaceBackgroundBrush}" CornerRadius="12" Padding="16">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Board.CurrentPlayer, StringFormat=Lượt hiện tại: {0}}"
                                                   FontSize="16" FontWeight="Bold"
                                                   Foreground="{DynamicResource AccentForegroundBrush}"/>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="Thời gian còn lại: "
                                                       Foreground="{DynamicResource DefaultForeground}"/>
                                            <TextBlock Text="{Binding RemainingTimeDisplay}"
                                                       FontWeight="Bold"
                                                       Foreground="{DynamicResource AccentForegroundBrush}"/>
                                        </StackPanel>
                                    </Grid>

                                    <TextBlock Grid.Row="1" Text="{Binding StatusMessage}"
                                               Margin="0,0,0,12"
                                               Foreground="{DynamicResource DefaultForeground}"/>

                                    <ScrollViewer Grid.Row="2"
                                                  HorizontalScrollBarVisibility="Auto"
                                                  VerticalScrollBarVisibility="Auto">
                                        <Viewbox Stretch="Uniform"
                                                 StretchDirection="UpOnly"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center">
                                            <ItemsControl ItemsSource="{Binding Board.Cells}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <UniformGrid Rows="{Binding Board.Rows}" Columns="{Binding Board.Columns}"/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>

                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Button Content="{Binding Value}"
                                                                Command="{Binding ClickCommand}"
                                                                Width="30" Height="30"
                                                                Margin="1"
                                                                FontWeight="Bold"
                                                                FontSize="16"
                                                                Foreground="{DynamicResource DefaultForeground}">
                                                            <Button.Style>
                                                                <Style TargetType="Button">
                                                                    <Setter Property="Background" Value="{DynamicResource CellBackgroundBrush}"/>
                                                                    <Setter Property="BorderBrush" Value="{DynamicResource CellBorderBrush}"/>
                                                                    <Setter Property="BorderThickness" Value="1"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsLastMove}" Value="True">
                                                                            <Setter Property="Background" Value="{DynamicResource LastMoveBrush}"/>
                                                                            <Setter Property="BorderBrush" Value="{DynamicResource Primary}"/>
                                                                            <Setter Property="BorderThickness" Value="2"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding IsWinningCell}" Value="True">
                                                                            <Setter Property="Background" Value="{DynamicResource WinningCellBrush}"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </Viewbox>
                                    </ScrollViewer>
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>
                </TabItem>

                <!-- Hướng dẫn -->
                <TabItem Header="📖  Hướng dẫn">
                    <Border Background="{DynamicResource SectionBackgroundBrush}" Margin="-4" Padding="16">
                        <ScrollViewer>
                            <StackPanel>
                                <TextBlock Text="Luật chơi Caro (Gomoku / Renju)" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource DefaultForeground}"/>
                                <Separator Margin="0,8"/>

                                <TextBlock Text="• Mục tiêu: đạt 5 quân liên tiếp theo hàng ngang, dọc hoặc chéo." Foreground="{DynamicResource DefaultForeground}"/>
                                <TextBlock Text="• Lượt đi: X luôn đi trước, O đi sau." Margin="0,6,0,0" Foreground="{DynamicResource DefaultForeground}"/>
                                <TextBlock Text="• Bàn cờ: mặc định 30×30, phù hợp chơi freestyle." Margin="0,6,0,0" Foreground="{DynamicResource DefaultForeground}"/>

                                <Separator Margin="0,8"/>
                                <TextBlock Text="Các biến thể luật" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource AccentForegroundBrush}" Margin="0,6,0,0"/>

                                <TextBlock Text="• Freestyle (Tự do):" FontWeight="Bold" Foreground="{DynamicResource DefaultForeground}" Margin="0,4,0,0"/>
                                <TextBlock Text="   - Xếp được 5 hoặc nhiều hơn quân liên tiếp đều thắng." Foreground="{DynamicResource DefaultForeground}"/>
                                <TextBlock Text="   - Không có luật cấm, đây là kiểu chơi phổ biến nhất ngoài đời." Foreground="{DynamicResource DefaultForeground}"/>

                                <TextBlock Text="• Standard Gomoku (Chuẩn):" FontWeight="Bold" Foreground="{DynamicResource DefaultForeground}" Margin="0,6,0,0"/>
                                <TextBlock Text="   - Chỉ đúng 5 quân liên tiếp mới thắng." Foreground="{DynamicResource DefaultForeground}"/>
                                <TextBlock Text="   - Nếu tạo hơn 5 quân (6, 7…), không tính thắng." Foreground="{DynamicResource DefaultForeground}"/>
                                <TextBlock Text="   - Không có luật cấm đặc biệt." Foreground="{DynamicResource DefaultForeground}"/>

                                <TextBlock Text="• Renju (Quốc tế):" FontWeight="Bold" Foreground="{DynamicResource DefaultForeground}" Margin="0,6,0,0"/>
                                <TextBlock Text="   - X phải tuân theo luật cấm để đảm bảo công bằng." Foreground="{DynamicResource DefaultForeground}"/>
                                <TextBlock Text="   - Cấm overline: không được xếp 6 quân trở lên." Foreground="{DynamicResource DefaultForeground}"/>
                                <TextBlock Text="   - Cấm double-three: không được tạo đồng thời 2 thế tam mở." Foreground="{DynamicResource DefaultForeground}"/>
                                <TextBlock Text="   - Cấm double-four: không được tạo đồng thời 2 thế tứ." Foreground="{DynamicResource DefaultForeground}"/>
                                <TextBlock Text="   - O không bị cấm, chỉ cần đạt 5 quân là thắng." Foreground="{DynamicResource DefaultForeground}"/>
                                <TextBlock Text="   - Đây là luật chính thức trong các giải đấu quốc tế." Foreground="{DynamicResource DefaultForeground}"/>

                                <Separator Margin="0,8"/>
                                <TextBlock Text="Mẹo nhanh" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource AccentForegroundBrush}" Margin="0,6,0,0"/>
                                <TextBlock Text="• Ưu tiên đi gần trung tâm và giữ thế mở hai đầu." Foreground="{DynamicResource DefaultForeground}"/>
                                <TextBlock Text="• Chặn ngay khi đối thủ có 4 quân liên tiếp." Foreground="{DynamicResource DefaultForeground}"/>
                                <TextBlock Text="• Tìm cách tạo hai mối đe dọa cùng lúc để ép thắng." Foreground="{DynamicResource DefaultForeground}"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </TabItem>

                <!-- Cài đặt -->
                <TabItem Header="⚙️  Cài đặt">
                    <Border Background="{DynamicResource SectionBackgroundBrush}" Margin="-4" Padding="16">
                        <StackPanel>
                            <TextBlock Text="Tùy chỉnh giao diện" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource DefaultForeground}"/>
                            <Separator Margin="0,8"/>
                            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                <TextBlock Text="Theme:" Width="160" Foreground="{DynamicResource DefaultForeground}"/>
                                <ComboBox Width="160" ItemsSource="{Binding Themes}" SelectedItem="{Binding SelectedTheme}"/>
                            </StackPanel>

                            <CheckBox Content="Âm thanh" Margin="0,6,0,0"
                                      IsChecked="{Binding IsSoundEnabled}"
                                      Foreground="{DynamicResource DefaultForeground}"/>

                            <Button Content="Lưu giao diện" Margin="0,16,0,0" Width="160"
                                    Background="{StaticResource Primary}" Foreground="{DynamicResource DefaultForeground}"
                                    Command="{Binding SaveSettingsCommand}"/>
                        </StackPanel>
                    </Border>
                </TabItem>

            </TabControl>
        </Border>
    </Grid>
</Window>
